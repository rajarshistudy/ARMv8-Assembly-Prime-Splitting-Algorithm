# ARMv8-Assembly-Prime-Splitting-Algorithm
Prime-splitting algorithm implemented in ARMv8 Assembly — manual stack frame management, byte-offset pointer arithmetic across three arrays, and GDB debugging to validate register state and memory contents before and after execution.
